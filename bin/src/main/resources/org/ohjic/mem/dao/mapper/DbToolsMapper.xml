<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ohjic.mem.dao.DbToolsMapper">

   <select id="selectTableIndexListByScheme" parameterType="string" resultType="org.ohjic.mem.model.InformationSchemaStatitics">
   SELECT DISTINCT
    TABLE_NAME tableName,
    COLUMN_NAME tableName,
    INDEX_NAME indexName
	FROM INFORMATION_SCHEMA.STATISTICS
	WHERE TABLE_SCHEMA = #{scheme}
   </select>
   
   <!-- 상품정보 삭제 -->
   <select id="selectTableIndexListBySchemeByInformationSchemaStatitics" parameterType="org.ohjic.mem.model.InformationSchemaStatitics" resultType="org.ohjic.mem.model.InformationSchemaStatitics">
   SELECT DISTINCT
    TABLE_NAME tableName,
    COLUMN_NAME tableName,
    INDEX_NAME indexName
	FROM INFORMATION_SCHEMA.STATISTICS
	WHERE TABLE_SCHEMA = #{church} and TABLE_NAME = #{tableName} and COLUMN_NAME=#{columnName} and INDEX_NAME=#{indexName}
   </select>
   
</mapper>